# @neondatabase/auth declares a peer of next>=16 while this project runs
# Next 15.1.3. It works — but npm's strict peer resolution refuses to install.
#
# Remove once Next is upgraded to 16. Neon's own docs already assume 16 (they
# name the middleware file proxy.ts), so the package is written against it.
legacy-peer-deps=true
